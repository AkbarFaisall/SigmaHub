name: scholarhub_sigma
description: Aplikasi sistem informasi beasiswa SIGMA.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  supabase_flutter: ^2.5.5
  image_picker: ^1.0.7
  image_cropper: ^12.2.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
  